NEXT STEP: publish svr-verify to npm
=====================================
Status as of 2026-06-06:
  - PyPI 1.0.3 published and verified (pip install svr-verify -> 1.0.3,
    total_time_ms and evaluation both in EXCLUDED_FIELDS).
  - Go module tag go/v1.0.3 pushed to GitHub.
  - npm is the only registry left.

The npm account is already logged in on this machine. The publish was
blocked only by npm's two-factor requirement (error E403, needs an OTP).

To finish, run from the js folder and supply the current 6-digit code
from your authenticator app (replace 123456 with the live code):

    cd C:\Dev\kha\sigma\github\svr-verify\js
    npm publish --otp=123456

Then confirm it is live:

    npm view svr-verify version          (should print 1.0.3)

Note: npm and PyPI never allow reusing a version filename, even after a
delete. If 1.0.3 is already up, do not republish it; bump to 1.0.4 for
any further change.
